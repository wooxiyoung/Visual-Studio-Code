
var random;
random = Math.floor(Math.random() * 10) + 1; // 1 ~ n 까지 범위내에서 랜덤하게 숫자 하나 뽑아주는 애.
var s = "주사위를 굴려 ( " + random + " ) 이 나왔습니다.";
document.write(s);

/* 4면체 */
var random2;
random2 = Math.floor(Math.random() * 100) + 1; // 1 ~ n 까지 범위내에서 랜덤하게 숫자 하나 뽑아주는 애.
var s2 = "주사위를 굴려 ( " + random2 + " ) 이 나왔습니다.";
document.write(s2);
